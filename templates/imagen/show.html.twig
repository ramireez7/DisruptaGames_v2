{% extends 'base.html.twig' %}

{% block title %}Detalle de imagen{% endblock %}

{% block principal %}

    <h1>Detalle de imagen</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ imagen.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ imagen.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ imagen.descripcion }}</td>
            </tr>
            <tr>
                <th>Categoria</th>
                <td>{{ imagen.categoria }}</td>
            </tr>
            <tr>
                <th>NumVisualizaciones</th>
                <td>{{ imagen.numVisualizaciones }}</td>
            </tr>
            <tr>
                <th>NumLikes</th>
                <td>{{ imagen.numLikes }}</td>
            </tr>
            <tr>
                <th>NumDownloads</th>
                <td>{{ imagen.numDownloads }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_imagen_index') }}">back to list</a>

    <a href="{{ path('app_imagen_edit', {'id': imagen.id}) }}">edit</a>

    {{ include('imagen/_delete_form.html.twig') }}
{% endblock %}
